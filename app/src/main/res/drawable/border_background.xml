<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- res/drawable/border_background.xml -->
    <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
        <!-- Outer border -->
        <item>
            <shape android:shape="rectangle">
                <solid android:color="#F4F2F2" /> <!-- Background color -->
                <stroke
                    android:color="#000000"
                    android:width="1dp" /> <!-- Border color and width -->
            </shape>
        </item>
        <!-- Inner padding to create the border effect -->
        <item android:left="1dp" android:right="1dp" android:top="1dp" android:bottom="1dp">
            <shape android:shape="rectangle">
                <solid android:color="#FFFFFF" /> <!-- Background color, same as outer border -->
            </shape>
        </item>
    </layer-list>
</selector>
